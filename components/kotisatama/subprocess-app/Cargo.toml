[package]
name = "kotisatama-subprocess-app"
version = "0.1.0"
edition.workspace = true
authors = ["Ilio / Kotisatama"]
license = "MPL-2.0"
publish = false
description = "Shared subprocess startup and health-check helpers for Kotisatama bundled apps"

[lib]
name = "kotisatama_subprocess_app"
path = "src/lib.rs"

[dependencies]
ureq = { version = "2.12", features = ["json"] }
